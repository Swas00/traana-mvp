services:
  - type: web
    name: traana-disaster-response
    env: node
    plan: free
    buildCommand: npm --prefix backend install && npm --prefix frontend install --include=dev && npm --prefix frontend run build
    startCommand: node backend/server.js
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
